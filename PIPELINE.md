# X KOL Sourcing 完整管线

从零到一张可投放的 KOL 表，共 6 个阶段。本文是流程与判断标准的单一真相源；可执行脚本目前覆盖 Stage 3（画像验证），其余阶段由 agent 按本文档驱动工具完成。

```
Stage 0 画像定义 → Stage 1 搜寻 → Stage 2 硬门槛筛选 → Stage 3 画像生成+逐人验证 → Stage 4 去重 → Stage 5 落表
```

---

## Stage 0 — 画像定义（人做，agent 不猜）

开跑前必须由需求方明确三件事，写进任务 brief：

1. **这条线是什么用途**：投放线（看互动/受众质量）还是建联线（看内容契合，互动可放宽）。同一硬门槛不通用——AFP 建联线曾按学者画像只捞到 5 人，放宽为"专业背景博主（bio/legal/quant/医学，PhD 或从业者）+ 亲 AI"后补到 14 人
2. **目标 ICP**：对齐品牌红线里的 creator 标准（触达真实 ICP：AI scientist/eval、biotech/pharma R&D、enterprise/institution、technical founders/university labs、verifiable-task 用户；⛔ 只看粉丝数、⛔ 泛 AI tools-list 号做复杂产品传播）
3. **池子结构**：按 campaign 分区（如 General / 预测 / AFP），跨池统一用 10 类类型体系归类（见 SCHEMA.md）

## Stage 1 — 搜寻（工具：twitterapi 为主）

- **主力**：`deepline tools execute twitterapi_advanced_search`（关键词组合 + `lang:zh` + `from:handle` 拉时间线，$0.001/条结果，全程可用不限流）
- **查询设计**：按画像拆关键词组（身份自述句式如"作为律师""读博" + 专业工具词如 qlib/Zotero/冷冻电镜 + 主题词），多组并行；**命中所有关键词组的号 = keyword-stuffing 农场，直接弃**
- **种子扩散**：已知优质号的转发/引用圈、榜单反查（榜单本身不可信，反查每个人）
- **辅助**：serper（主页/身份验证）；**⛔ 别默认走 Apify**——LinkedIn/主页验证曾占掉 Deepline 78% 花费，先用 serper/twitterapi 过滤再上贵工具
- **中文区特别注意**：华人学者基本在英文区发推，`lang:zh` 捞不到，要学者得单独跑英文查询；医药关键词矿脉（临床试验/FDA/创新药）被伪科学健康号+政治号严重污染，逐条人审

## Stage 2 — 硬门槛筛选（可脚本化的一票否决）

数值门槛（投放线默认，建联线可放宽）：

- 粉丝 ≥ 10K（专业稀缺方向可降至 3K）
- 活跃：14 天内有发帖、近 5 周原创 ≥ 10 条（专业博主天然低频：21 天内活跃、5 周 ≥6 条）
- 互动：原创推文**中位数**赞（不用均值，防置顶爆款拉偏）；**高粉低赞 = 买粉硬信号**（10万+ 粉中位赞 ≤ 个位数直接拒，案例：189K 粉中位赞 1）

内容一票否决（中文区实战沉淀）：

- 名字/bio 挂交易所推广后缀（"买美股上XX""上XX预测世界杯" = 付费挂名号）
- 瓜分 XX U 抽奖、邀请码/返佣链接、"自动印钞机/认知差"课程腔、八字风水测球
- 盗版资源分发、短信接码等灰产工具推广、在线赌场推广、金狗喊单+钱包跟单导流
- 乱码型 handle + 空 bio + 新号高粉（农场三件套）

正则命中 ≠ 定罪：**劝退式引用会误伤**（"别想一夜暴富"命中暴富词），命中必须回读原文定性——曾有两个优质号因此误杀后捞回。

## Stage 3 — 画像生成 + 逐人时间线验证（本 repo 脚本覆盖）

**这是整条管线的血泪教训所在**：2026-07 中文区池 93 人复核，发现建表时按"账号名+关键词命中"生成的画像列错误率 ~40%（13 个硬伤：农场号被写成科研博主、负债学量化的奶爸被写成 LegalTech 分析师、甚至把期望用途写成了事实描述）。

铁律：**画像四列（身份/具体做法/主要内容/受众）必须以逐人完整时间线为输入生成，否则标"未验证"**。写得越具体越顺的画像越可能是编的。

执行（见 README 快速开始）：

```
fetch_sheet.py → extract_handles.py → pull_timelines.sh → digest.py → agent 逐人判断 → apply_edits.py
```

判断标准与三档处置（✅属实 / 🔧修正 / 🔴删除候选）见 AGENTS.md。产出必须带**弃选口径**：筛掉的人列明原因，供 owner 复核（自我净化规则）。

## Stage 4 — 去重与排除

写表前对三份名单去重：

1. 既有各池（含英文种子表）
2. vendor 名单（vendor 给的名单里"筛选列为空/负面"的单独分区处理，不混主表；vendor 铺的 UGC 热帖不算 organic 信号）
3. **已合作红人排除名单**（维护在 `data/exclude_list.txt`，gitignore，向 owner 索取）——已经合作过的不再当候选

## Stage 5 — 落表（Google Sheet）

- 表结构与维度见 **SCHEMA.md**（11 基础列 + 评估五列 + 分区约定）
- 通道：Google Sheets MCP 或 Apps Script webhook（凭据问表格 owner 要，走 .env，不进 git；具体实现不在本文档展开）
- **改表纪律（不可违反）**：
  1. Sheet 是唯一真相源——owner 会手动删人/改人，写前必须读回现状，**永远不用本地快照整页覆盖**
  2. 只做定点增量（改单元格 / 末尾追加 / 标记指定行）
  3. 删除候选只标不删，删行由 owner 执行
  4. 备注列只前置追加，原文用｜保留
  5. 每次写完汇报改动明细供 owner 核对
- 表内类型列若是下拉菜单（Sheets Table 格式），列类型只能 owner 在 UI 手动配置，脚本只写值

---

## 工具与成本速查

| 环节 | 工具 | 成本 | 备注 |
|---|---|---|---|
| 搜寻/拉时间线 | deepline `twitterapi_advanced_search` | $0.001/条 | 主力，93 人全量验证 ≈ $2 |
| 身份/主页验证 | serper | 极低 | 先于一切贵工具 |
| LinkedIn/深度爬取 | Apify | 贵 | ⛔ 不默认用，先过滤再上 |
| 判断/画像 | Claude Code / Codex agent | — | 按 AGENTS.md 跑 |
| 落表 | Sheets MCP / webhook | — | 凭据不进 git |

历史参考：单轮中文区搜寻 ~5K 推文全流程 ≈ 14 credits（$1.4）；Reddit 数据**永远不走 API app 注册那条路**。
